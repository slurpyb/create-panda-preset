import { defineSemanticTokens, type SemanticTokens } from '@pandacss/dev';

import type { Tokens } from '@pandacss/dev';

/**
 * Gradient tokens.
 */
export const gradients: SemanticTokens['gradients'] = defineSemanticTokens.gradients({});
